# 感知层SDK配置更新与构建验证计划

## 📋 任务概述

更新感知层SDK的配置文件，集成新的IP摄像头和DashScope API配置，并验证构建过程。

## 🎯 配置信息

### 1. IP摄像头连接参数
- **视频流地址**: `http://172.31.2.252:8081/video`
- **访问凭据**: admin/123456

### 2. DashScope API服务
- **API密钥**: `sk-fb1a1b32bf914059a043ee4ebd1c845a`

## 📝 实施步骤

### 步骤1: 定位感知层SDK的配置文件

**目标**: 找到感知层SDK的配置文件位置

**操作**:
1. 搜索感知层模块中的配置文件
   - 查找 `config.json`
   - 查找 `.env` 文件
   - 查找 `yaml` 格式配置文件
   - 查找 `PerceptionConfig.kt` 中的默认配置

2. 确认配置文件格式
   - 检查是否使用JSON格式
   - 检查是否使用Properties格式
   - 检查是否使用YAML格式

**预期结果**: 找到所有需要更新的配置文件

---

### 步骤2: 更新IP摄像头连接参数

**目标**: 按照SDK文档规范更新摄像头连接字符串和认证信息

**操作**:
1. 更新IP摄像头URL
   - 旧值: `http://192.168.1.100:8080/video`
   - 新值: `http://172.31.2.252:8081/video`

2. 更新IP摄像头用户名
   - 旧值: `admin`
   - 新值: `admin` (保持不变)

3. 更新IP摄像头密码
   - 旧值: `password`
   - 新值: `123456`

**文件清单**:
- `local.properties` (项目根目录)
- `Perception-SDK-Integration-Guide.md` (文档)
- 任何其他包含配置的文件

**预期结果**: 所有配置文件中的IP摄像头参数已更新

---

### 步骤3: 配置DashScope API服务

**目标**: 在API配置段添加DashScope密钥，确保加密存储或环境变量隔离

**操作**:
1. 更新DashScope API密钥
   - 旧值: `your-dashscope-api-key` 或 `your-api-key`
   - 新值: `sk-fb1a1b32bf914059a043ee4ebd1c845a`

2. 验证密钥格式
   - 确认密钥以 `sk-` 开头
   - 确认密钥长度正确
   - 确认密钥没有多余空格

3. 安全性检查
   - 确保密钥不会提交到git (检查.gitignore)
   - 确认使用环境变量或加密存储
   - 验证配置文件权限

**预期结果**: DashScope API密钥已正确配置并安全存储

---

### 步骤4: 更新文档中的配置示例

**目标**: 确保所有文档中的配置示例与实际配置一致

**操作**:
1. 更新 `Perception-SDK-Integration-Guide.md`
   - 更新所有代码示例中的配置
   - 更新所有配置说明
   - 确保示例代码可运行

2. 更新 `CONFIGURATION.md` (如果存在)
   - 更新配置说明
   - 添加新的配置项
   - 更新使用示例

3. 更新 `local.properties.template`
   - 更新模板文件
   - 添加注释说明
   - 确保模板可复用

**预期结果**: 所有文档与实际配置一致

---

### 步骤5: 执行清理构建

**目标**: 保存配置后执行清理构建，确保使用最新配置

**操作**:
1. 清理Gradle缓存
   ```bash
   ./gradlew clean
   ```

2. 清理构建输出
   ```bash
   rm -rf build/
   rm -rf .gradle/
   ```

3. 同步依赖
   ```bash
   ./gradlew --refresh-dependencies
   ```

**预期结果**: 构建环境已清理，准备重新构建

---

### 步骤6: 验证构建日志无错误

**目标**: 确认所有依赖正确解析，构建成功

**操作**:
1. 执行完整构建
   ```bash
   export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
   export PATH="$JAVA_HOME/bin:$PATH"
   ./gradlew build --no-daemon
   ```

2. 检查构建日志
   - 查找 `BUILD SUCCESSFUL` 消息
   - 检查是否有编译错误
   - 检查是否有依赖解析错误
   - 检查是否有配置错误

3. 记录构建时间
   - 记录总构建时间
   - 记录各模块构建时间
   - 识别性能瓶颈

**预期结果**: 构建成功，无错误和警告

---

### 步骤7: 运行单元测试和集成测试

**目标**: 确保视频流采集与AI分析链路正常

**操作**:
1. 运行单元测试
   ```bash
   ./gradlew test --no-daemon
   ```

2. 运行集成测试
   ```bash
   ./gradlew connectedAndroidTest --no-daemon
   ```

3. 验证测试覆盖
   - 检查感知层测试覆盖率
   - 检查配置加载测试
   - 检查API调用测试

4. 检查测试结果
   - 确认所有测试通过
   - 记录失败的测试
   - 分析失败原因

**预期结果**: 所有测试通过，视频流采集与AI分析链路正常

---

### 步骤8: 输出构建报告

**目标**: 生成包含版本号、构建时间、测试通过率等关键指标的报告

**操作**:
1. 收集构建信息
   - Git提交ID: `git rev-parse HEAD`
   - 构建时间戳
   - Gradle版本
   - Kotlin版本

2. 收集测试信息
   - 单元测试通过率
   - 集成测试通过率
   - 测试覆盖率

3. 收集配置信息
   - IP摄像头URL
   - DashScope API密钥 (脱敏)
   - 刷新间隔

4. 生成报告
   ```markdown
   # 构建报告

   - **版本**: [提交ID]
   - **构建时间**: [时间戳]
   - **构建状态**: 成功/失败
   - **测试通过率**: [百分比]
   - **配置验证**: 通过/失败
   ```

5. 保存报告
   - 保存到 `build-reports/` 目录
   - 命名为 `build-report-[timestamp].md`

**预期结果**: 完整的构建报告已生成

---

## 📊 验证清单

### 配置验证
- [ ] IP摄像头URL已更新为 `http://172.31.2.252:8081/video`
- [ ] IP摄像头用户名已更新为 `admin`
- [ ] IP摄像头密码已更新为 `123456`
- [ ] DashScope API密钥已更新为 `sk-fb1a1b32bf914059a043ee4ebd1c845a`
- [ ] 所有配置文件已同步更新
- [ ] 配置文件已添加到.gitignore

### 构建验证
- [ ] Gradle缓存已清理
- [ ] 依赖已刷新
- [ ] 构建成功完成
- [ ] 无编译错误
- [ ] 无依赖解析错误
- [ ] 构建时间在合理范围内 (<10分钟)

### 测试验证
- [ ] 单元测试全部通过
- [ ] 集成测试全部通过
- [ ] 测试覆盖率 > 80%
- [ ] 视频流采集测试通过
- [ ] AI分析链路测试通过

### 文档验证
- [ ] 所有文档配置示例已更新
- [ ] 文档与实际配置一致
- [ ] 配置说明清晰完整
- [ ] 构建报告已生成

---

## 🚨 风险与应对

### 风险1: 配置文件格式错误
**应对**: 在更新前备份原配置文件，更新后验证格式正确性

### 风险2: API密钥泄露
**应对**: 确保.gitignore包含配置文件，使用环境变量存储敏感信息

### 风险3: 构建失败
**应对**: 逐步构建，先构建核心模块，再构建功能模块

### 风险4: 测试失败
**应对**: 分析失败原因，修复后重新运行测试

---

## 📈 预期成果

1. **配置完整性**: 所有配置文件已更新，IP摄像头和DashScope API配置正确
2. **构建成功**: 项目可以成功构建，无错误和警告
3. **测试通过**: 所有单元测试和集成测试通过
4. **文档同步**: 所有文档与实际配置一致
5. **构建报告**: 生成详细的构建报告，包含所有关键指标

---

## 📝 备注

- 本计划遵循SDK文档规范
- 配置更新遵循最小权限原则
- 构建过程遵循CI/CD最佳实践
- 测试覆盖遵循质量保证标准
